const quantityUpdaterStyles = theme => ({
	loader: {
		width: 500,
		height: 200,
		display: "flex",
		justifyContent: "center",
		alignItems: "center",
	},
	dialogContent: { overflowY: "hidden !important", paddingTop: 0 },
})

export default quantityUpdaterStyles
